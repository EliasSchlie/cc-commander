name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  node:
    name: ${{ matrix.component }} (Node)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        component: [hub, runner]
    defaults:
      run:
        working-directory: ${{ matrix.component }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: ${{ matrix.component }}/package-lock.json
      - run: npm ci
      - run: npm run typecheck
      - run: npm test

  swift:
    name: Swift package + Xcode builds
    runs-on: macos-15
    defaults:
      run:
        working-directory: client/swift
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.app

      - name: Run Swift package tests
        working-directory: client/swift/CCCommanderPackage
        run: swift test

      - name: Build macOS app
        run: |
          xcodebuild build \
            -project CCCommander.xcodeproj \
            -scheme CCCommander_macOS \
            -destination 'platform=macOS' \
            -quiet

      - name: Build iOS app
        # Use generic iOS device target so we don't need a simulator runtime
        # installed on the runner (saves ~1GB download).
        run: |
          xcodebuild build \
            -project CCCommander.xcodeproj \
            -scheme CCCommander_iOS \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO \
            -quiet
